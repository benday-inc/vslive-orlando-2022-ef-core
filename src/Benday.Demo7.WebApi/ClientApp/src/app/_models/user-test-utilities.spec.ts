import { UserTestUtilities } from './user-test-utilities';

describe('UserTestUtilities', () => {
  it('should create an instance', () => {
    expect(new UserTestUtilities()).toBeTruthy();
  });
});
