import { PersonTestUtilities } from './person-test-utilities';

describe('PersonTestUtilities', () => {
  it('should create an instance', () => {
    expect(new PersonTestUtilities()).toBeTruthy();
  });
});
