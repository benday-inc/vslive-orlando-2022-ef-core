import { LookupTestUtilities } from './lookup-test-utilities';

describe('LookupTestUtilities', () => {
  it('should create an instance', () => {
    expect(new LookupTestUtilities()).toBeTruthy();
  });
});
