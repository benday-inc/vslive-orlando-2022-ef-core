import { LogEntryTestUtilities } from './log-entry-test-utilities';

describe('LogEntryTestUtilities', () => {
  it('should create an instance', () => {
    expect(new LogEntryTestUtilities()).toBeTruthy();
  });
});
