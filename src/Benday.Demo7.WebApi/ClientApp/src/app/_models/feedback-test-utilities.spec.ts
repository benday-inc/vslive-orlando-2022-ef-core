import { FeedbackTestUtilities } from './feedback-test-utilities';

describe('FeedbackTestUtilities', () => {
  it('should create an instance', () => {
    expect(new FeedbackTestUtilities()).toBeTruthy();
  });
});
