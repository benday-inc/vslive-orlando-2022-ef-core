import { ConfigurationItemTestUtilities } from './configuration-item-test-utilities';

describe('ConfigurationItemTestUtilities', () => {
  it('should create an instance', () => {
    expect(new ConfigurationItemTestUtilities()).toBeTruthy();
  });
});
