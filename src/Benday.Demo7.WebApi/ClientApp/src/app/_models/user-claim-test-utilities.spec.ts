import { UserClaimTestUtilities } from './user-claim-test-utilities';

describe('UserClaimTestUtilities', () => {
  it('should create an instance', () => {
    expect(new UserClaimTestUtilities()).toBeTruthy();
  });
});
