import { MockRoutingHelperService } from './mock-routing-helper-service';

describe('MockRoutingHelperService', () => {
  it('should create an instance', () => {
    expect(new MockRoutingHelperService()).toBeTruthy();
  });
});
