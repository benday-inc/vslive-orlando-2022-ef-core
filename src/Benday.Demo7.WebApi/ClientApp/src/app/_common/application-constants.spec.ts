import { ApplicationConstants } from './application-constants';

describe('ApplicationConstants', () => {
  it('should create an instance', () => {
    expect(new ApplicationConstants()).toBeTruthy();
  });
});
