import { StringToBooleanPipe } from './string-to-boolean-pipe';

describe('StringToBooleanPipe', () => {
  it('should create an instance', () => {
    expect(new StringToBooleanPipe()).toBeTruthy();
  });
});
